{
  "name": "pharos-prototype-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev --prefix pharos-backend\" \"npm run dev --prefix pharos-frontend\"",
    "install:all": "npm install --prefix pharos-backend && npm install --prefix pharos-frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
